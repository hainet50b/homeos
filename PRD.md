# PRD: homeos

This is the Product Requirements Document. It owns three things: **What** the product is, **Why** it exists, and the open and closed **Tasks**. End-user-facing surface lives in `README.md`; internal structure lives under `SPEC/`; coding style and Tech Stack live in `CONVENTIONS.md`.

## What

An AI-agent-driven CLI that keeps install scripts reproducible across machines, managed as a single Git repository — package definitions in `homeos.yml`, action scripts under `packages/<name>/`, plugin bundles under `plugins/<name>/`. See `README.md` for the user-facing specification and `SPEC/SPEC.md` for the internal data model.

## Why

Reproducing a developer's software setup across machines today requires either re-running install commands by hand or adopting heavyweight configuration management frameworks (Ansible, NixOS, etc.) that swallow the broader codebase. homeos sits in the gap: it is the minimum-overhead layer that captures install commands as Git-backed text, drivable end-to-end by an AI agent through generated `AGENTS.md` guidance, with no service to host and no abstraction to learn beyond the file layout.

## Tasks

- [x] Scaffold Cargo project with clap CLI skeleton (`homeos --help` works). Base directory must be injectable so tests use a `tempdir` instead of the real data directory.
- [x] Implement `homeos.yml` parsing with serde (packages with `script_aliases` and `enabled`)
- [x] Implement `homeos init` — create directory structure and empty `homeos.yml`
- [x] Migrate from `serde_yaml` to `yaml_serde` and update all existing code and tests
- [x] Refactor all existing unit tests to follow the 3A pattern (Arrange / Act / Assert)
- [x] Implement `homeos cd` — launch a shell in the default repository directory
- [x] Implement `homeos package list` — list all packages from `homeos.yml`
- [x] Implement `homeos package add <pkg>` — create package directory and add entry to `homeos.yml`
- [x] Implement `homeos package remove <pkg>` — remove package entry from `homeos.yml`
- [x] Enhance `homeos package add` to generate skeleton action scripts (install/update/uninstall with OS-appropriate extension). Each script should contain a comment noting it was generated by homeos and needs to be filled in.
- [x] Implement `homeos package enable <pkg>` — remove `enabled: false` from package
- [x] Implement `homeos package disable <pkg>` — set `enabled: false` on package
- [x] Implement confirmation prompt — show plan with disabled package messages, prompt `Proceed? [y/N]`. Separate plan logic from stdin so the plan can be unit tested independently. — show plan with disabled package messages, prompt `Proceed? [y/N]`
- [x] Implement `homeos package install <pkg>...` — execute install action scripts
- [x] Implement `homeos package update <pkg>...` — execute update action scripts
- [x] Implement `homeos package uninstall <pkg>...` — execute uninstall action scripts
- [x] Implement `homeos package cat <pkg>` — display all action scripts with filename headers. Show `(not found)` for missing scripts.
- [x] Implement `state.yml` parsing with serde (installed packages list)
- [x] Enhance `homeos init` to generate `.gitignore` that excludes `state.yml`
- [x] Enhance `homeos package install` to record installed packages in `state.yml`. Create `state.yml` if it does not exist.
- [x] Enhance `homeos package uninstall` to remove uninstalled packages from `state.yml`
- [x] Enhance `homeos package install` to skip already-installed packages. Pass `state.yml` into `Plan::build` so it classifies installed packages separately. Display `Skipping <pkg> (already installed)` in the plan and do not execute the install script for those packages.
- [x] Refactor install/update/uninstall to update `state.yml` per package (not at the end). On script failure, report the error and continue to the next package instead of aborting. Successfully installed packages must be recorded in `state.yml` even if a later package fails. Same for uninstall — remove from `state.yml` per package after successful execution.
- [x] Enhance `homeos package uninstall` to disable uninstalled packages in `homeos.yml` after successful execution.
- [x] Fix `homeos package add` to preserve existing scripts when the package directory already exists. Only generate skeleton scripts for files that are missing.
- [x] Add `--all` flag to `homeos package uninstall` to uninstall all installed packages (from `state.yml`). Show confirmation prompt with the full list before execution.
- [x] Fix `homeos package uninstall` to ignore disabled status. Uninstall should always execute regardless of whether the package is enabled or disabled.
- [x] Extend `Plan` with a `not_installed` classification. Refactor `run_action` to pass `state.yml` to `Plan::build` for all actions (not just install).
- [x] Implement the full behavior matrix (see README Behavior section). `update` skips not-in-state packages. `uninstall` skips not-in-state packages but ignores disabled status.
- [x] Refactor action from `&str` to an `Action` enum (`Install`, `Update`, `Uninstall`). Eliminate string comparisons and default branches.
- [x] Enhance `homeos package enable` to accept multiple packages (`<pkg>...`).
- [x] Enhance `homeos package disable` to accept multiple packages (`<pkg>...`).
- [x] Enhance `homeos package remove` to reject packages that are in `state.yml`. Error with a message to uninstall first.
- [x] Enhance `homeos package list` to show a table with Enabled and Installed columns, referencing both `homeos.yml` and `state.yml`.
- [x] Split `commands/package.rs` into `commands/package/mod.rs`, `commands/package/registry.rs` (list, add, remove, enable, disable, cat), and `commands/package/action.rs` (run_action, execute_script, resolve_script_name, update_state_per_package). Each module keeps its own tests and fixtures.
- [x] Extract `script_extension() -> &'static str` into `commands/package/mod.rs` to eliminate scattered `cfg!(windows)` checks.
- [x] Rename `confirm.rs` to `plan.rs` to better reflect its responsibility (Plan, Action, confirmation prompt).
- [x] Reorder functions and methods to match the command definition order in README (list, add, remove, enable, disable, cat, cd, install, update, uninstall).
- [x] Move `install`, `update`, `uninstall`, and `uninstall_to` from `commands/package/mod.rs` to `commands/package/action.rs`. Keep `mod.rs` limited to re-exports and shared helpers (`script_extension`, `shell_command`).
- [x] Add `depends_on` field to `PackageConfig` in config.rs (optional `Vec<String>`, skipped when empty in serialization)
- [x] Add `--depends-on` option to `homeos package add` to specify dependencies at creation time
- [x] Implement `homeos package add-dep <pkg> <dep>...` — add dependencies to an existing package
- [x] Implement `homeos package remove-dep <pkg> <dep>...` — remove dependencies from a package
- [x] Enhance `homeos package remove` to reject packages that are depended on by other packages. Error with a message listing the dependents.
- [x] Rename CLI argument name for add-dep and remove-dep from `dep` to `dependency` for clarity in help output.
- [x] Implement topological sort for package install order based on `depends_on`. Error on circular dependencies.
- [x] Integrate dependency ordering into `homeos package install` — install dependencies before the requested package
- [x] Implement `homeos apply` — install packages that are enabled + not in state, update packages that are enabled + in state
- [x] Integrate dependency ordering into `homeos apply` — respect `depends_on` when determining install order
- [x] Add `--repo` option to CLI — select which repository to operate on (defaults to `default`)
- [x] Implement `homeos repo list` — list registered repositories
- [x] Implement `homeos repo add <name> <url>` — clone a remote repository into `repos/<name>/`
- [x] Implement `homeos repo remove <name>` — remove the local repository directory
- [x] Enhance `homeos apply` to include disabled packages in the plan display as `Skipping <pkg> (disabled)` instead of silently filtering them out.
- [x] Enhance `homeos repo remove` to reject removal if the repository's `state.yml` contains installed packages. Error with a message to uninstall first.
- [x] Implement `homeos package cd [<package>]` — launch a shell in the package root or specific package directory
- [x] Enhance `homeos init` to accept an optional `<url>` argument to clone a remote repository as the default repo
- [x] Implement `--strip-git` flag for `homeos init` — remove `.git` directory after cloning
- [x] Add `plugins` section to Config (serde: `BTreeMap<String, PluginConfig>` with `url` field)
- [x] Add `plugin` (optional `String`) and `params` (optional `BTreeMap<String, String>`) fields to PackageConfig
- [x] Implement `homeos plugin list` — list registered plugins in the current repository
- [x] Implement `homeos plugin list-remote` — fetch `hainet50b/homeos-plugin-*` repositories from GitHub API, display name, description, and URL in a table. Show error message on network failure.
- [x] Implement `homeos plugin add <name> [<url>]` — register plugin in `homeos.yml` and clone into `plugins/<name>/`. Without URL, resolve as `https://github.com/hainet50b/homeos-plugin-<name>`.
- [x] Implement `homeos plugin remove <name>` — remove `plugins/<name>/` directory and entry from `homeos.yml`. Warn (but do not block) if packages reference this plugin.
- [x] Add `--plugin <name>` and `--params <key=value>...` options to `homeos package add` CLI definition
- [x] Integrate plugin into `homeos package add` — when `--plugin` is specified, load `params.yml` from `plugins/<plugin>/`, read the OS-appropriate template (`<action>.sh.tmpl` or `<action>.ps1.tmpl`), replace `{{key}}` placeholders with `params` values, and write the result as the action script. Skip actions whose templates do not exist for the current OS. Record `plugin` and `params` in `homeos.yml`.
- [x] Fix shell detection in `cd.rs` and `package/registry.rs` for Windows support. Use `SHELL` env var if set, otherwise `pwsh` on Windows, `/bin/sh` on Unix. Extract the shared logic into a common helper to eliminate duplication.
- [x] Validate cloned repository in `homeos init <url>` — after clone, check that `homeos.yml` exists. If not, remove the cloned directory and error with "Not a valid homeos repository".
- [x] Validate cloned plugin in `homeos plugin add` — after clone, check that `params.yml` exists. If not, remove the cloned directory and error with "Not a valid homeos plugin".
- [x] Add URL existence check before `git clone` in `homeos plugin add` (when URL is auto-resolved). Use GitHub API to verify the repository is public. Error with a clear message if not found.
- [x] Implement `--local` flag for `homeos plugin add` — create an empty plugin skeleton (`params.yml` + OS-appropriate template files) in `plugins/<name>/` for local development.
- [x] Remove `.git` directory from cloned plugins after `homeos plugin add` (same approach as `init --strip-git`).
- [x] Warn when executing unmodified skeleton scripts. Before execution, check if the script contains "Generated by homeos". If so, display a warning inline in the plan (e.g., `neovim (warning: install.sh is unmodified)`). Do not block execution.
- [x] Respect dependency order in `homeos package uninstall` — expand dependencies and topologically sort, then execute in reverse order (dependents first, then their dependencies).
- [x] Move `check_unmodified_scripts` logic into `Plan::build` so that warnings are computed at construction time. Remove `&mut self` method and make `Plan` immutable after build.
- [x] Ensure all commands produce meaningful stdout output describing what was done. Review every command and add output where missing for consistency.
- [x] Rename plugin config file from `params.yml` to `plugin.yml`. Update all references: `plugin add --local` skeleton generation, `plugin add` validation check, `package add` manifest loading, `config.rs` PluginManifest, and all related tests.
- [x] Implement `homeos plugin cat <name>` — display all template files and `plugin.yml` for the specified plugin with filename headers.
- [x] Implement `homeos plugin cd [<name>]` — launch a shell in the plugins root directory, or in the specific plugin directory if name is given.
- [x] Fix `homeos plugin add --local` to generate all template files (.sh.tmpl and .ps1.tmpl) regardless of the current OS. OS-specific selection happens at `package add` time, not plugin creation time.
- [x] Unify plan display format — change `Skipping <pkg> (reason)` to `The following packages will be skipped:` with `<pkg> (reason)` listed underneath, matching the style of install/update/uninstall sections.
- [x] Show plugin name in plan display — append `(plugin: <name>)` to packages that use a plugin.
- [x] Change `homeos package add` to deny if the package directory already exists, instead of adding only missing scripts. Show a clear error message suggesting to remove the existing directory first.
- [x] Enhance `homeos package remove` to accept multiple packages (`<packages>...`). Update help message to "Remove package entries from homeos.yml".
- [x] Rename CLI argument placeholders and variable names: plugin commands `name` → `plugin`, repo commands `name` → `repo`. Update help output, variable names, and call sites.
- [x] Change `homeos plugin remove` to keep the plugin directory (only remove the entry from `homeos.yml`), consistent with `package remove`.
- [x] Guard `homeos repo remove` against deleting the `default` repository. Error with a clear message.
- [x] Rename `script_aliases` to `script_aliases` in `PackageConfig`, `homeos.yml` serde, and all related code and tests.
- [x] Add `--script-aliases` option to `homeos package add` to specify script aliases at creation time (e.g., `--script-aliases update=install`).
- [x] Implement `homeos package add-alias <pkg> <alias>...` — add script aliases to an existing package (e.g., `update=install`).
- [x] Implement `homeos package remove-alias <pkg> <alias>...` — remove script aliases from a package by target name (e.g., `update`).
- [x] Change `homeos package add` to generate skeleton scripts for all OS (both `.sh` and `.ps1`) regardless of the current OS.
- [x] Change `homeos package cat` to display all script files (both `.sh` and `.ps1`) regardless of the current OS.
- [x] Fix `PackageConfig::default()` so that `enabled` defaults to `true`. Currently `#[derive(Default)]` sets it to `false`. Implement `Default` manually or adjust the struct to ensure `enabled` is `true` by default.
- [x] Add confirmation prompt to `homeos repo remove` before deleting the repository directory.
- [x] Allow `homeos repo add <name>` without URL to create an empty local repository (same structure as `init`).
- [x] Implement `homeos repo cd [<repo>]` — launch a shell in the specified repository directory (defaults to `default`).
- [x] Change `homeos cd` to launch a shell in the base directory (`<data_dir>/homeos`) instead of the default repository.
- [x] Change `homeos cd` to launch a shell in the repos directory (`<data_dir>/homeos/repos`) instead of the base directory.
- [x] Change `execute_script` from `.output()` to `.status()` so that scripts can interact with the user's terminal (stdin/stdout/stderr inherited). Update tests to use file-based side effects instead of stdout capture for verification.
- [x] Add `--purge` flag to `homeos package remove` to delete the package directory in addition to removing the `homeos.yml` entry.
- [x] Add `--purge` flag to `homeos plugin remove` to delete the plugin directory in addition to removing the `homeos.yml` entry.
- [x] Add confirmation prompt to `homeos package remove` before removing entries. Show which packages will be removed from `homeos.yml`. When `--purge` is used, also show which directories will be deleted.
- [x] Add confirmation prompt to `homeos plugin remove` before removing entries. Show which plugins will be removed from `homeos.yml`. When `--purge` is used, also show which directories will be deleted.
- [x] Add a newline after `Installing <name>...` (and `Updating`, `Uninstalling`) before script execution, so that script output starts on its own line. `done` should also be on its own line after execution.
- [x] `homeos init` scaffold mode: error if the repo directory already exists (even without homeos.yml).
- [x] Unify plan display for all commands: always call `plan.display()` regardless of whether there are enabled packages. When `plan.is_empty()`, display the skipped section followed by `Nothing to do.` and exit without a confirmation prompt. Remove per-command `No packages to {action}.` messages. This applies to `apply`, `install`, `update`, and `uninstall`.
- [x] Change script execution output order from `{verb} {name}...` / `FAILED` / `Error: ...` to `{verb} {name}...` / `Error: ...` / `FAILED`. Error details should appear before the conclusion.
- [x] Change `Some packages failed` to stdout and exit code 0. The message is a summary report, not a fatal error. `run_action` should report individual failures to stdout during execution, print `Some packages failed` to stdout at the end, and return `Ok(())`.
- [x] Change `homeos init` "Already initialized" from `println!` (stdout) to `eprintln!` (stderr) with `Error:` prefix, and return `Err`.
- [x] Append ". Cloned directory removed." to the error message in `homeos init` when cloned repo has no homeos.yml.
- [x] Append ". Cloned directory removed." to the error message in `homeos plugin add` when cloned plugin has no plugin.yml.
- [x] Change `homeos cd` error message from "Repos directory not found" to "Repositories directory not found".
- [x] Change `homeos package list`, `homeos plugin list`, and `homeos repo list` to show table headers even when empty, instead of "No packages." / "No plugins." / "No repositories." messages.
- [x] Add existence check for `homeos.yml` in `Config::load`. If the file does not exist, return a clear error: `homeos.yml not found at {path}. Run 'homeos init' first.`
- [x] Change `--depends-on`, `--script-aliases`, and `--params` in `homeos package add` from `num_args = 1..` to `action = Append` to avoid consuming subsequent options. Rename `--script-aliases` to `--script-alias` and `--params` to `--param` (singular, matching append-per-call semantics).
- [x] Change "deleted directory" to "removed directory" in `homeos package remove --purge` and `homeos plugin remove --purge` output messages.
- [x] Implement reverse dependency expansion for `homeos package uninstall`. When uninstalling B, find all packages that depend on B (recursively) and include them in the plan. Execute dependents first, then dependencies. Show dependents in the plan with a note (e.g., `A (depends on B)`).
- [x] Add `Dependencies` column to `homeos package list` table output.
- [x] Implement `homeos package info <package>` — display package details: enabled, installed, plugin, params, dependencies, dependents, script aliases.
- [x] Add dependency target validation to `homeos package add-dep` and `homeos package add --depends-on`. Error if the specified dependency does not exist in `homeos.yml`.
- [x] Add circular dependency check to `homeos package add-dep` and `homeos package add --depends-on`. Error if adding the dependency would create a cycle.
- [x] Enhance `homeos package info` to show a Scripts section listing all script files (both `.sh` and `.ps1`). For existing scripts, show the filename and full path. For missing scripts, show the filename and `(not found)`.
- [x] Change `add-dep` messages to match command argument order: "Package '{name}' now depends on '{dependency}'" (success), "Package '{name}' already depends on '{dependency}'" (already exists).
- [x] Change `remove-dep` messages to match command argument order: "Package '{name}' no longer depends on '{dependency}'" (success), "Package '{name}' does not depend on '{dependency}'" (not found).
- [x] Create `plugins/` directory during `homeos init`, consistent with `packages/` directory creation.
- [x] Handle circular dependencies gracefully in `apply`, `install`, and `uninstall`. Instead of aborting entirely, skip packages involved in the cycle with reason `(circular dependency)` in the plan, and proceed with the remaining packages.
- [x] Extract git clone invocation into a shared helper. Create a `git` module with a `clone(url, target)` function that wraps the `Command::new("git")` call and error handling (`git clone failed: {stderr}`). Migrate `homeos init`, `homeos repo add`, and `homeos plugin add` to use it.
- [x] Split `commands/plugin.rs` into `commands/plugin/mod.rs` and submodules, following the same structure as `commands/package/`. Group list/list-remote/add/remove into one submodule and cat/cd into another. Each submodule keeps its own tests and fixtures.
- [x] Add `#[command(version)]` attribute to the `Cli` struct in `main.rs` so that `homeos --version` and `homeos -V` display the version from `Cargo.toml`.
- [x] Implement `homeos package rename <old> <new>` — rename the package directory on disk and update the entry key in `homeos.yml`. Update `state.yml` if the package is installed. Update any `depends_on` references in other packages to point to the new name. Error if a package with the new name already exists.
- [x] Add `--dry-run` flag to `homeos apply`, `homeos package install`, `homeos package update`, and `homeos package uninstall`. When set, display the plan and exit successfully without prompting for confirmation or executing scripts. This supports automation and AI agent workflows where non-interactive plan inspection is required.
- [x] Change `PluginConfig.url` from `String` to `Option<String>`. Plugins created via `plugin add --local` must be serialized without a `url` field in `homeos.yml` (no `url: ''`). Use `#[serde(skip_serializing_if = "Option::is_none")]` so `None` is omitted on write. Backward compatibility with existing `url: ''` entries is explicitly NOT required — the project is pre-release with no external users, so the cleanest representation takes precedence. Update all consumers (clone logic in `plugin add`, plugin lookup, `plugin list` display) to handle `Option<String>`. In `plugin list`, render `(local)` in the URL column when the URL is `None`. Update tests accordingly.
- [x] Replace `reqwest` with `ureq` as the HTTP client. Update `Cargo.toml` (remove `reqwest`, add `ureq` with the `json` feature) and migrate the two call sites in `homeos plugin list-remote` (GitHub Search API fetch) and `homeos plugin add` (URL existence check) to use ureq's synchronous API. Update the Tech Stack section of this PRD to reference ureq instead of reqwest. Update tests accordingly.
- [x] Change `Context::new` in `src/context.rs` to use `dirs::data_local_dir()` instead of `dirs::data_dir()`. On Windows this resolves the base directory to `%LOCALAPPDATA%\homeos` (matching the README spec) instead of the current `%APPDATA%\homeos` (which is the Roaming directory and would incorrectly sync machine-specific `state.yml` and plugin clones across machines via Windows account roaming). On Linux and macOS the behavior is unchanged because `data_dir()` and `data_local_dir()` return the same path. Update the test at `src/context.rs:213` (and any other test that references `dirs::data_dir()`) to use `data_local_dir()`. Also update the Data Model section of this PRD to say `data_local_dir()` instead of `data_dir()`.
- [x] Annotate forward-dependency packages in the install plan with `(required by <requester>)` to mirror the existing reverse-dependency annotation `(depends on <package>)` on the uninstall plan. When `homeos package install A` (or `homeos apply`) pulls in `B` because `A` depends on `B`, render `B (required by A)` in the plan. For transitive expansion (A → B → C), annotate each pulled-in dependency with its most direct requester (C is required by B, B is required by A). This applies to `homeos package install` and `homeos apply` which both perform forward expansion. Reuse the existing `notes` field on `Plan`. Update tests accordingly.
- [x] Annotate intra-set dependencies in `homeos apply` plans with `(required by <requester>)`. Currently the install plan rendered by `apply` does not show `(required by X)` notes because `expand_dependencies` treats packages already in the input set as "explicitly requested" and skips annotation. For `apply`, all packages come from the enabled set in `homeos.yml`, not from explicit user request, so any dependency relationship within the resulting set should be visible. In `apply_to`, after the existing `expand_dependencies` call, walk the final ordered package list and for each package find the most direct requester among the other packages in the set; if found, set the `(required by <requester>)` note on the plan. The behavior of `homeos package install` is unchanged. Update tests accordingly.
- [x] In install plans (whether invoked via `homeos package install` or `homeos apply` for not-yet-installed packages), skip any package whose dependency chain includes a disabled package, with annotation `(dependency disabled: <name>)` where `<name>` is the most direct unavailable dependency. Currently if A depends on B and B is disabled, B is correctly skipped with `(disabled)` but A is still installed, which is likely to fail because A's install script relies on B being installed. After classifying disabled packages in `Plan::build`, perform a propagation pass for `Action::Install`: walk each enabled package's `depends_on` graph; if any dependency is disabled (directly or transitively skipped via this same rule), classify the package as skipped with `(dependency disabled: <most_direct_unavailable_dep>)`. Update is unaffected because `update` does not expand dependencies and assumes a dep's installed state is already correct on the machine (tracked via state.yml), independent of its enabled flag. Uninstall is unaffected because it ignores disabled status. Update `Plan`, `Plan::display`, the Plan Display section of COMMAND_OUTPUT.md, and tests accordingly.
- [x] Fix duplicate `The following packages will be skipped:` header in `homeos apply` output. When package A (enabled) depends on package B (disabled), `expand_dependencies` pulls B into `expanded_install`, then `Plan::build` classifies B as disabled, and `plan.display()` renders a skipped section listing it. Separately, `apply_to` collects all disabled packages at the start of the function and renders its own skipped section after the plan display block, producing two consecutive `The following packages will be skipped:` headers. Fix by filtering disabled packages out of `expanded_install` after the `expand_dependencies` call so they no longer reach `install_names` / `Plan::build`, leaving `apply_to`'s top-level `disabled_packages` list as the single source of truth for the skipped section. Update tests accordingly.
- [x] Convert unmodified skeleton script detection from "warn and execute" to "skip and do not execute". When a package's action script (e.g., `install.sh` for the install action) still contains the `Generated by homeos` marker comment, classify the package as skipped with `(script unmodified: <filename>)` instead of rendering `(warning: <filename> is unmodified)` and executing. Applies to install, update, and uninstall (each checks the script for its own action). `state.yml` is NOT modified for skipped packages — this prevents creating false `installed` state for packages with unfilled install scripts (the silent failure that the previous warn-but-execute behavior produced). Remove the `warnings` field from `Plan` since unmodified detection was its only use case. Update `Plan`, `Plan::build`, `Plan::display`, the Plan Display section of COMMAND_OUTPUT.md, README examples that reference the warning form, and tests accordingly.
- [x] In `homeos apply` output, when an enabled+in-state package has an unmodified skeleton script for the update action, the package must appear in the consolidated skipped section of the plan display with the same `(script unmodified: <filename>)` annotation that `homeos package update` already produces. Currently `apply` silently skips execution of such packages without displaying them, so the user has no signal that the package was excluded. The single `The following packages will be skipped:` header invariant from PRD #211 must be preserved — no duplicate header even when install-side and update-side skipped entries co-exist. Update tests accordingly, including coverage for the apply path with a mix of install-side and update-side skipped entries.
- [x] In `homeos package uninstall <pkg>`, the uninstall plan must include only the requested packages and any other packages that transitively depend on them (reverse dependencies). It must NOT include the requested packages' forward dependencies. Currently the plan also pulls in forward dependencies — for example, `uninstall A` where `A` depends on `B` schedules both `A` and `B` for uninstall, which incorrectly removes packages the user did not request and may still need. The existing behavior of pulling in dependents (e.g., `uninstall B` where `A` depends on `B` also schedules `A`) must be preserved. Update tests accordingly, including a regression test that uninstalls a package with forward dependencies and asserts only the requested package and its dependents (not its dependencies) appear in the plan.
- [x] Sort the output of `homeos plugin list-remote` alphabetically by plugin name. Currently the output preserves the order returned by the GitHub Search API, which is opaque and subject to change. The local `plugin list` already sorts alphabetically (via `BTreeMap` iteration), so this brings the remote listing in line with that UX, makes test assertions stable, and lets the README document the order deterministically. Update tests accordingly.
- [x] Implement `homeos plugin info <plugin>` to display a plugin's metadata: name, URL (or `(local)` for plugins added via `--local`), parameters, and the list of action template files that exist (e.g., `install.sh.tmpl`, `install.ps1.tmpl`). The output style mirrors the existing `homeos package info <package>` command. Error when the plugin is not found in `homeos.yml`. The `## homeos plugin info` section of `COMMAND_OUTPUT.md` has been written ahead of time and serves as the specification — match its wording exactly. In `README.md`, add a new `#### \`homeos plugin info\`` subsection under `## Manage plugins`, following the existing `homeos package info` subsection's format. Do not modify the Quick Tour, Using a plugin example, Features, or any other curated sections of `README.md`. Update tests accordingly.
- [x] Add a required `description` (string) field to the top level of `plugin.yml`. The field carries a human-readable one-line summary of the plugin's purpose. Backward compatibility with the previous schema (no `description` field) is explicitly NOT required — the project is pre-release with no external users. `plugin add --local` skeleton generation must emit a placeholder `description` value (e.g., `"Brief description of what this plugin does."`) so freshly created plugins are immediately valid. `plugin list` output must add a `Description` column populated from each plugin's `plugin.yml`. The `homeos plugin info` command must include the `description` in its output. `plugin list-remote` continues to use the GitHub repository About text and is not affected. Update `COMMAND_OUTPUT.md` for `plugin list` and `plugin info`. In `README.md`, update the `homeos plugin list` example output and the `plugin.yml` example in the Plugin Development Guide section; do not modify the Quick Tour, Official Plugins table, or any other curated sections. Update tests accordingly.
- [x] Implement `homeos completion <shell>` that prints a shell completion script for the given shell to stdout. Supported shells: bash, zsh, fish, powershell, elvish (via the `clap_complete` crate), and nushell (via the `clap_complete_nushell` crate). The command does not write any files; output goes to stdout so the caller can redirect to the shell-specific location. The `## homeos completion` section of `COMMAND_OUTPUT.md` has been written ahead of time and serves as the specification — match its wording exactly. Update tests accordingly.
- [x] Extend `install.sh` and `install.ps1` to auto-install the shell completion file for the user's detected shell after the homeos binary itself is placed. `install.sh` detects `$SHELL` and handles bash, zsh, fish, elvish, and nushell. `install.ps1` handles PowerShell. For each shell, place the completion file in the conventional side-effect-free location (creating the parent directory as needed): bash → `~/.local/share/bash-completion/completions/homeos`, zsh → `~/.local/share/zsh/site-functions/_homeos`, fish → `~/.config/fish/completions/homeos.fish`, PowerShell → `$env:USERPROFILE\.homeos\completion.ps1`, elvish → `~/.config/elvish/lib/homeos.elv`, nushell → `~/.config/nushell/completions/homeos.nu`. The install scripts must NOT modify any rc / profile / config file (`.bashrc`, `.zshrc`, `$PROFILE`, `config.nu`, `rc.elv`, etc.). After installation, the script must print stdout guidance describing any additional manual step the user needs to take to activate the completion — for example, adding the directory to `fpath` for zsh, sourcing the completion file from `$PROFILE` for PowerShell, sourcing from `config.nu` for nushell, or `use` from `rc.elv` for elvish. For fish (and bash when `bash-completion` is installed), no further action is required, so the guidance should be a simple "completion installed" confirmation. If `$SHELL` does not match any of the supported shells, skip completion setup entirely without any message — the user is on something we cannot help with via `homeos completion` regardless. Update tests accordingly.
- [x] Fix the URL column separator in `homeos plugin list` and `homeos plugin list-remote` output. Currently the separator under the `URL` header is fixed at three dashes (`---`, matching the header width), while the `Name` and `Description` columns size their separators to the widest content. Make the URL separator match the widest URL value in the rows, consistent with the other columns. Update tests accordingly.
- [x] Fix the Dependencies column separator in `homeos package list` output. Currently the separator under the `Dependencies` header is fixed at the header width (12 dashes from `"-".repeat(deps_header.len())`), while the `Package` column already sizes its separator to the widest content. The `Enabled` and `Installed` columns are safe because their values (`yes`/`no`) never exceed their headers, but Dependencies values can exceed the header — e.g., `bubblewrap, socat` is 17 characters. Make the Dependencies separator match the widest value in the rows, consistent with the Package column. Update tests accordingly.
- [x] Drop `homeos repo` subcommands and the `--repo` global flag from the CLI. Removes the `Commands::Repo` variant, the `RepoCommands` enum, and the dispatch arm in `src/main.rs`. Deletes `src/commands/repo.rs` and its module declaration. `Context::new` continues to accept `repo: String` for now but `main.rs` always passes `"default".to_string()`. Directory layout is unchanged in this task — packages and plugins still live at `<data_dir>/repos/default/...`. Tests for `commands/repo.rs` are removed; other tests are unchanged.
- [x] Flatten the directory layout so that `homeos.yml`, `state.yml`, `.gitignore`, `packages/`, and `plugins/` live directly under the data directory instead of `repos/default/`. Rename `Context.base_dir` to `Context.data_dir` for terminology consistency with the spec, and update `Context::new`'s signature to `Context::new(data_dir: Option<PathBuf>) -> Self`. Drop the `repo` field from `Context`, drop `Context::repos_dir()` and `Context::repo_dir()`, and update `Context::packages_dir()` / `config_path()` / `state_path()` / `plugins_dir()` / `gitignore_path()` to resolve to `data_dir.join(...)` directly. Update `homeos init` (both scaffold and clone modes) and `homeos cd` for the flat layout. The `Directory Structure`, `homeos init`, and `homeos cd` sections of `README.md`, and the `## homeos init` / `## homeos cd` tables of `COMMAND_OUTPUT.md`, are the specification — match the user-visible behavior and error wording exactly. No migration code or backward-compat shims for the old `repos/default/` layout — the project is pre-release with the maintainer as the only existing user, and a clean break is preferred. Update all callers and tests accordingly.
- [x] Honor the `HOMEOS_DATA_DIR` environment variable in `Context::new`. Resolution priority: (a) explicit `data_dir` argument (used by tests and any future programmatic override), (b) `HOMEOS_DATA_DIR` env var if set, (c) `dirs::data_local_dir().join("homeos")` default. The env var value is used as the data directory verbatim (no `homeos/` segment appended). The `Overriding the data directory` section of `README.md` is the specification — match its described behavior. Tests must isolate from the ambient `HOMEOS_DATA_DIR` (e.g., via the `serial_test` crate or explicit `env::set_var` / `env::remove_var` symmetry) so that the developer's own shell environment does not leak into test outcomes.
- [x] Update `test-command-output.sh` for the flat data directory and `HOMEOS_DATA_DIR`-based isolation. Set `HOMEOS_DATA_DIR="$(mktemp -d)"` at the top of the script to isolate the test run from the developer's real homeos data, and remove all `--repo "$TEST_REPO"` arguments and the `$TEST_REPO` variable. Replace the `homeos repo add "$TEST_REPO"` setup and `homeos repo remove "$TEST_REPO"` teardown with plain `mkdir`/`rm -rf` against `$HOMEOS_DATA_DIR`. Remove the trailing `=== homeos repo list ===`, `=== homeos repo add (already exists) ===`, and `=== homeos repo remove (default) ===` example sections from the script.
- [x] Drop nushell from supported completion shells. Remove the `Nushell` variant from `CompletionShell` in `src/commands/completion.rs`, drop the nushell dispatch arm, remove the `clap_complete_nushell` dependency from `Cargo.toml`, drop the nushell branch from `install.sh` (the `$SHELL` ≈ `*/nu` case writing to `~/.config/nushell/completions/homeos.nu`), and drop nushell from the supported shells list in the `Shell completion` section of `README.md`. Update tests accordingly (delete the nushell-specific test cases and adjust `test_completion_help_lists_all_supported_shells`).
- [x] Switch `src/commands/completion.rs` from static script generation (`clap_complete::generate`) to dynamic completion via `clap_complete::engine`. Update `Cargo.toml` to `clap_complete = { version = "= 4.6.5", features = ["unstable-dynamic"] }` — the exact-version pin guards against API breakage in the unstable engine across minor releases. Call `clap_complete::CompleteEnv::with_factory(|| Cli::command()).complete()` at the top of `main()` so the binary intercepts completion subprocess invocations (via the `COMPLETE` env var) and emits candidates before normal arg parsing. The `homeos completion <shell>` subcommand remains and now emits the engine's registration snippet for the requested shell (a small script that re-invokes the binary for candidates) instead of a full static completion script. Rewrite the existing tests in `completion.rs` to assert the new registration-snippet format rather than the previous static-script markers (`#compdef`, `complete -c homeos`, etc.). No dynamic value completers are attached in this task — args still produce only the static candidates they did before.
- [x] Attach `ArgValueCompleter`s for package and plugin names. Add `Context::try_new() -> Option<Self>` that mirrors the resolution priority of `Context::new` (explicit `data_dir` argument > `HOMEOS_DATA_DIR` env var > `dirs::data_local_dir().join("homeos")`) but returns `None` when `dirs::data_local_dir()` is unavailable, so completion code paths never panic. Add `src/completers.rs` with two functions: `package_completer` reads `<data_dir>/homeos.yml` via `Context::try_new()` and `Config::load`, then returns `config.packages.keys()` as `CompletionCandidate`s; `plugin_completer` does the same for `config.plugins.keys()`. Any failure path (no `Context`, no `homeos.yml`, parse error) returns an empty `Vec` without panic or log. Attach these completers via `#[arg(add = ArgValueCompleter::new(...))]` to: positional `<PACKAGE>` / `<PACKAGES>...` on `package remove/rename/enable/disable/info/cat/cd/install/update/uninstall`, the first positional of `package add-dep/remove-dep/add-alias/remove-alias`, the `--depends-on` value of `package add`, the positional `<PLUGIN>` on `plugin remove/info/cat/cd`, and the `--plugin` value of `package add`. Unit-test each completer with a temporary `HOMEOS_DATA_DIR` containing a fixture `homeos.yml`; use the `serial_test` crate (or explicit `env::set_var` / `env::remove_var` symmetry) to avoid env var pollution between tests.
- [x] Verify the dynamic completion installation flow end-to-end. Confirm that `install.sh` and `install.ps1` continue to place the engine-emitted registration snippet at the conventional per-shell location with the same `homeos completion <shell> > <path>` redirection pattern as today. If the engine output for any shell requires a different invocation or activation step (e.g., a different file extension, sourcing instruction, or eval invocation), update the install scripts and the `Shell completion` section of `README.md` accordingly. Manual verification on fish (the maintainer's primary shell) is required to confirm that package and plugin name completion via TAB works after install, both from a fresh shell session and across `HOMEOS_DATA_DIR` overrides.
- [x] Add the global `--output <format>` flag (with `--json` as a shorthand for `--output json`) and the `HOMEOS_OUTPUT_FORMAT` env var to `Cli` in `src/main.rs`. Resolution priority: (a) explicit flag > (b) env var > (c) `text` default. Introduce an `OutputFormat` enum (`Text`, `Json`), thread it through `Context` (or a separate `OutputCtx` if cleaner), and build a small dispatcher so each command can call `ctx.emit(value)` or similar to render in the active format. No actual JSON serialization yet — the dispatch infrastructure is set up but every command still produces today's text output. Tests verify the flag/env var resolution priority.
- [x] Migrate error output to the dual stdout-JSON / stderr-human format whenever JSON mode is active. In JSON mode, errors emit `{"error": {"reason": "<kebab-id>", "message": "<text>"}}` to stdout AND the same `Error: <message>` text to stderr that text mode emits. In text mode, behavior is unchanged: no stdout output, `Error: <message>` text on stderr only. The contract is: **stdout differs between modes (structured JSON in JSON mode, empty in text mode), stderr is identical across modes** — this keeps human-facing wording consistent regardless of which mode the user invoked. Define the canonical set of `reason` kebab-case identifiers as part of this task and bake them into the error variants. The expected set (non-exhaustive): `package-not-found`, `plugin-not-found`, `already-exists`, `validation-error`, `circular-dependency`, `dependency-not-found`, `dependent-exists`, `script-failed`, `script-not-found`, `script-unmodified`, `git-clone-failed`, `not-a-valid-homeos-repo`, `not-initialized`, `data-dir-not-empty`, `data-dir-not-found`, `not-found-on-github`, `network-error`. Enumerate every error path in the codebase and assign a reason. The `## homeos init` and similar tables in `COMMAND_OUTPUT.md` will be updated by this task to reflect the new format. Update tests accordingly.
- [x] Implement JSON output for the list commands (`package list`, `plugin list`, `plugin list-remote`). Each emits a JSON array of objects when in JSON mode; the field set matches the columns of the existing text table. Update the `## homeos package list` / `## homeos plugin list` / `## homeos plugin list-remote` sections of `COMMAND_OUTPUT.md` with the JSON schema. Existing text table output remains unchanged.
- [x] Implement JSON output for the info commands (`package info`, `plugin info`). Each emits a single JSON object when in JSON mode; the field set covers everything shown in the current text output (enabled, installed, plugin, params, depends_on, dependents, script_aliases for package; description, url, parameters, templates for plugin). Update `COMMAND_OUTPUT.md` accordingly. Existing text output remains unchanged.
- [x] Implement JSON output for the plan display used by `apply`, `package install`, `package update`, `package uninstall`. Schema must capture the install/update/skipped sections, per-package annotations (`(disabled)`, `(already installed)`, `(required by X)`, `(depends on Y)`, `(circular dependency)`, `(dependency disabled: Z)`, `(script unmodified: F)`), and `is_empty`. `--dry-run --json` exits 0 with the plan on stdout. `--json` without `--dry-run` writes the plan, prompts as today (unless `--yes`), and then writes execution results as additional JSON objects (one per package, NDJSON style, with `status: "success" | "failed"`). Update the `Plan Display` section of `COMMAND_OUTPUT.md`.
- [x] Validate package and plugin names against a strict whitelist before any filesystem operation. Allowed pattern: `^[a-z0-9][a-z0-9._-]*$` (lowercase alphanumerics, dot, underscore, hyphen; must not start with `-` or `.`). Reject control characters, slashes, backslashes, `..`, NUL bytes, leading dashes (would be misinterpreted as flags), and whitespace. Apply to `<PACKAGE>`, `<PACKAGES>...`, `<PLUGIN>`, `<DEPENDENCY>...`, `--depends-on` values, and `--plugin` values. Validation errors use the canonical `validation-error` reason. Update tests with a malicious-input matrix (control chars, path traversal, leading dash, embedded slashes).
- [x] Validate URL inputs for `homeos init <url>` and `homeos plugin add <url>`. Reject control characters, percent-encoded NULs / `%2e%2e`, embedded query strings inside path segments, and any scheme other than `http`/`https`/`git`/`ssh`/`git+ssh`. Validation errors use the canonical `validation-error` reason. Update tests with a malicious-URL matrix.
- [x] Add the global `--yes` flag that skips the `Proceed? [y/N]` confirmation prompt on `apply`, `package install`, `package update`, `package uninstall`, `package remove`, and `plugin remove`. When `--yes` is supplied, the plan is still displayed (matching the current `--dry-run` plan format) but the prompt is skipped and execution proceeds immediately. Mutually compatible with `--json` (plan and results emit as JSON). Mutually compatible with `--dry-run` but `--dry-run` still wins (display only, no execution). Update tests for each command.
- [x] Implement `homeos agents-md` command that renders the AGENTS.md content to stdout. The command reads `templates/AGENTS.md.tmpl` (embedded at compile time via `include_str!`), substitutes `{{ version }}` with `env!("CARGO_PKG_VERSION")` and `{{ commands_reference }}` with an auto-generated per-command reference (one entry per leaf command in the `Cli::command()` tree, each containing the command path, short description, and arg/flag list), then writes the rendered Markdown to stdout. No file writes — same convention as `homeos completion <shell>`. As part of this task, create a minimal skeleton `templates/AGENTS.md.tmpl` containing only the `<!-- generated by homeos {{ version }} -->` marker on the first line, top-level section headers as placeholders, and the two `{{ ... }}` placeholders so the binary compiles and tests pass; the full prose content is filled by a separate task. Update tests to verify the version marker and `commands_reference` substitution.
- [x] Fill `templates/AGENTS.md.tmpl` with the comprehensive AI-agent guide content. The file is the canonical homeos-for-agents reference, shipped with the binary via `include_str!` (from the previous task). Content sections: (1) Overview — what homeos is, what this session is for, the `homeos cd` entry pattern. (2) Operating principles — `--json` / `--yes` / `--dry-run` usage, the dry-run → user-confirmation → `--yes` apply pattern, the convention to `git add -A && git commit -m "..."` after mutation, mutation safety in general. (3) Error JSON schema with the canonical `reason` identifiers enumerated. (4) Input safety — package/plugin name character allowlist, URL scheme allowlist. (5) Canonical workflows — five narrative walk-throughs including `"Install Neovim for me"` (OS detection → plugin selection → package add → dry-run → confirm → apply --yes → git commit). (6) OS-to-plugin mapping reference (dnf/apt → linux, homebrew → mac/linux, winget/scoop → windows, etc.). (7) Per-command reference — leave the `{{ commands_reference }}` placeholder unchanged; it is substituted at render time. (8) Brief plugin authoring note that defers to the main README for depth. (9) Local customizations — a closing section instructing the agent to also read `AGENTS.local.md` if it exists in the same directory, noting that homeos never modifies that file. Expected total rendered size ~600-700 lines.
- [x] Integrate AGENTS.md generation into `homeos init`. During init, render the AGENTS.md content via the same code path as `homeos agents-md` and write the rendered output to `<data_dir>/AGENTS.md`. Then create `<data_dir>/CLAUDE.md` so Claude Code's ancestor scan discovers the same content: on Linux/macOS create it as a symlink to `AGENTS.md`; on Windows write a copy of the rendered content instead, since symlinks require elevated privileges. Extend the `.gitignore` that `homeos init` writes to also ignore `AGENTS.md` and `CLAUDE.md` alongside the existing `state.yml` entry. `AGENTS.local.md` is NOT added to .gitignore — users may version-control it if they choose. Update tests.
- [x] Add version-aware AGENTS.md auto-refresh to `homeos cd`. Before launching the shell, read the first line of `<data_dir>/AGENTS.md`, parse the embedded version from the `<!-- generated by homeos X.Y.Z -->` marker, and compare with `env!("CARGO_PKG_VERSION")`. If the file is missing, unparseable, or the embedded version differs from the current binary's version, regenerate AGENTS.md (via the same code path as `homeos agents-md`) and refresh CLAUDE.md (re-create symlink on Linux/macOS; rewrite copy on Windows) before launching the shell. Print a one-line stderr notice `homeos: refreshed AGENTS.md to v<X.Y.Z>` on regeneration so the change is visible to the user. No-op if the version matches. Update tests.
- [x] Add a `## Using with AI agents` section to `README.md`. The section explains the canonical agent workflow: run `homeos cd` to enter the homeos data directory, then launch your AI agent (Claude Code, Codex, Aider, Cursor, etc.) from there. The agent automatically discovers `AGENTS.md` (or `CLAUDE.md` for Claude Code) at the working directory and learns how to operate homeos. Include one short example: a natural-language user prompt such as `"Install Obsidian for me"` and a brief description of how the agent uses `package list --json` / `plugin list-remote --json` to plan, presents the plan to the user, applies after confirmation, and commits to git. Also mention that users can create an optional `<data_dir>/AGENTS.local.md` file with their own agent guidance; homeos never touches that file, and the generated `AGENTS.md` instructs the agent to read `AGENTS.local.md` when present. Place the section after `## Official Plugins`. Ralph drafts the first version; the maintainer reviews and refines it (this is an explicit experiment in using Ralph for prose authoring).
- [x] Run `git init` inside `<data_dir>` as part of `homeos init`'s scaffold mode so the data directory is a Git repository from the moment it exists. The AGENTS.md content already assumes this — workflows reference `git add` / `git commit` directly. `homeos init` should not configure `user.name` / `user.email` (those belong to the user) and should not create any commits (the agent walks the user through the rest of the initial setup per the "First-time setup of a new repository" section in `AGENTS.md`). Clone mode (`homeos init <url>`) is unaffected because `git clone` already establishes the git state. Update tests.
- [x] Implement `homeos plugin refresh <plugin>` to refetch the on-disk plugin from its configured `url`. With `--all`, refresh every registered plugin in `homeos.yml` in one invocation; without `<plugin>` and without `--all`, error with a hint to specify one or the other (mirroring the shape of `homeos package uninstall <names>... | --all`). For each target: shallow-clone the plugin's `url` into a temporary directory, validate that `plugin.yml` is present (reuse the `not-a-valid-homeos-plugin` validation from `plugin add`), compute a byte-hash file-by-file diff between the temp directory and `plugins/<name>/`, and present a short summary (`M install.ps1.tmpl`, `A README.md`, `D obsolete.sh.tmpl`, etc.). If files differ, prompt for confirmation; `--yes` skips the prompt; `--dry-run` exits after showing the summary. On approval, atomically replace `plugins/<name>/` with the temp directory contents (stripping `.git` as `plugin add` does); the `homeos.yml` entry is untouched. Plugins registered without a `url` (added via `plugin add --local`) are skipped with a notice — they are hand-maintained. In `--all` mode, aggregate every plugin's summary into a single confirmation prompt at the end so the user sees the full picture and decides once. The `## homeos plugin refresh` section of `COMMAND_OUTPUT.md` will be pre-written and serves as the specification — match its wording exactly. Update tests.
- [x] Force the initial branch name to `main` when `homeos init` runs `git init` in scaffold mode. Currently `homeos init` invokes `git init` without specifying a branch, so the resulting branch name depends on the user's `init.defaultBranch` config — on a freshly set-up machine without that config, Git's old default of `master` is used and a deprecation hint is printed. Change the invocation to `git init --initial-branch=main` (Git 2.28+). Document the requirement on Git 2.28 or newer in the README's installation prerequisites if it isn't already. Clone mode (`homeos init <url>`) is unaffected because `git clone` already establishes whatever branch the upstream uses. The rationale is that the homeos data directory is a homeos-managed repository, not the user's personal project, so picking `main` as a consistent convention across every homeos installation is reasonable; users who disagree can rename with `git branch -m main <other>` once. Update tests.
- [x] Force `core.autocrlf=false` for every `git` invocation made by homeos. On Windows, Git for Windows ships with `core.autocrlf=true` as its installer default, which rewrites LF to CRLF on checkout. This breaks `homeos plugin refresh` in two ways: (1) the freshly cloned plugin templates land on disk with CRLF while the locally registered copy was written with LF, so `compute_diff` reports every plugin as "modified" even when the content is byte-identical, and (2) the rendered shell scripts (`install.sh` etc.) end up with CRLF line endings that fail when executed under bash / WSL. The Windows job of the `Build` workflow has been red since v0.3.2 for exactly the first reason — `test_run_to_reports_up_to_date_when_no_changes` and adjacent `plugin refresh` tests in `src/commands/plugin/refresh.rs` create an upstream repo with LF content, clone it through the production `git::clone` path, and assert "up to date", which never holds when the cloned working tree has been CRLF-rewritten. The fix is to inject `-c core.autocrlf=false` into the `git` argv inside `src/git.rs` for both `git::clone` (`Command::new("git").args(["-c", "core.autocrlf=false", "clone", url, &target.to_string_lossy()])`) and `git::init` (the same `-c` prefix in front of `init --initial-branch=main ...`) so the data directory and every plugin checkout are byte-faithful regardless of the user's global git config. This is a production-quality fix, not just a CI workaround — Windows users running `homeos plugin refresh` need LF-preserving clones for the generated install scripts to be executable. No user-facing output changes, so `COMMAND_OUTPUT.md` and `README.md` need no updates. Existing tests do not need to be relaxed; they should keep passing on Linux/macOS and become green on Windows. Confirm the fix by running `cargo test` locally on Linux (Windows verification happens via the `Build` workflow on push).
- [x] Detect the available Windows shell at action-execution time and prefer `pwsh` (PowerShell 7+) when on PATH, falling back to `powershell.exe` (Windows PowerShell 5.1) which ships preinstalled on every modern Windows install. Currently homeos hard-codes `pwsh`, so on a fresh Windows machine the first attempt to run any `.ps1` action fails — including the very `homeos package install powershell` invocation a user would write to fix the situation, which is a chicken-and-egg bootstrap failure that defeats the "manage your install scripts" purpose. The shell selection lives next to `script_extension()` in `src/commands/package/mod.rs`; thread the detected shell through `execute_script` so the chosen binary is invoked. Add a `(running under Windows PowerShell 5.1; PowerShell 7 recommended)` annotation to the plan display when the fallback is active on a Windows host, so the user sees the constraint before script execution. Update tests with `#[cfg(windows)]` integration coverage that asserts the fallback path is taken when `pwsh` is absent. Documentation in `README.md` (the Plugin Development Guide section on PowerShell 5.1-compatible template authoring) and in `templates/AGENTS.md.tmpl` (the note steering agents away from PowerShell 7-only syntax) is intentionally out of scope for this task — the maintainer edits those by hand outside the Ralph loop to keep the prose voice consistent.
- [x] Add a version-check short-circuit to both `install.sh` and `install.ps1` so a curl-pipe-sh / iwr-pipe-iex re-run on a machine that's already on the latest release exits early without downloading or overwriting anything. Each script checks whether `homeos` is on PATH, runs `homeos --version`, fetches the latest tag from `https://api.github.com/repos/$REPO/releases/latest` (parsing `tag_name` with `sed` in shell and `Invoke-RestMethod` in PowerShell — no `jq` or other external dependency), and exits 0 with a message ("homeos X.Y.Z is already the latest. Set HOMEOS_FORCE_INSTALL=1 to reinstall.") when they match. The `HOMEOS_FORCE_INSTALL` environment variable, when set to any non-empty value, skips the check and proceeds with the install — useful for forced reinstall after a binary corruption or for installing a specific older tag manually. Network failure on the GitHub API call must NOT block install — fall through to the normal download path so the script remains usable offline / behind firewalls / when GitHub is down. The check runs before any temp directory is created or any download is attempted, so the no-op case is fast (one HTTPS round-trip).
- [x] Replace the direct `Move-Item -Force` overwrite of `$InstallDir\homeos.exe` in `install.ps1` with the Windows installer rename-then-replace pattern. Windows holds an exclusive lock on a running `.exe`, so `Move-Item -Force` fails when the user re-runs the installer with `homeos.exe` open in any shell — including from another terminal window the user has forgotten. The rename approach (used by Chrome, VS Code, and most Windows installers): if `$InstallDir\homeos.exe` already exists, rename it to `homeos.exe.old-<8-hex>` first (Windows allows renames of running exes even when it forbids overwrites), then move the new binary into place. After the new binary is installed, attempt to remove any `homeos.exe.old-*` files in the directory — those left over from prior runs are now unlocked. On a `Remove-Item` failure (the user is still running the previous version somewhere), silently ignore and let the next install clean up. If the initial rename itself fails (rare — happens when antivirus or specific shell history holds an exclusive handle), error out with a clear message asking the user to close any running `homeos` processes and any terminals where homeos was recently invoked, then re-run. `install.sh` is unaffected — Linux / macOS file semantics already allow replacing a running binary because the existing inode keeps the running process alive while a new inode takes the path.
- [x] Add a best-effort "update available" notification to `homeos cd`. Before launching the shell, check whether a newer release exists on GitHub and surface it on stderr; otherwise stay silent and continue. The check fetches `https://api.github.com/repos/hainet50b/homeos/releases/latest` with `ureq` using a 1500 ms timeout. Cache the result in `<data_dir>/.last-update-check` as a small JSON file with two fields: `last_checked_at` (Unix epoch seconds, integer — avoids pulling in `chrono` for timestamp formatting) and `latest_tag` (string, e.g., `"v0.3.8"`). Use `serde_json` (already a dep) for serialization and a dedicated struct (e.g., `UpdateCheckCache`) so future fields can be added without breaking parsing. Cache-handling rules: file missing or JSON malformed → treat as cache miss, do network check, write the file; file exists and `now - last_checked_at < 7 * 24 * 60 * 60` seconds → reuse the cached `latest_tag` without a network call; file exists and stale → refetch and rewrite. **Always update `last_checked_at` to the current time after a check attempt, regardless of network outcome** — this is the bounded-cost guarantee for offline / airgapped machines so they don't pay the 1500 ms timeout on every cd, only once per 7-day window. On network failure (timeout, DNS, non-200, parse failure), preserve the previous `latest_tag` if the cache file existed, or seed it with the current binary's tag (`v` + `env!("CARGO_PKG_VERSION")`) if the file was missing. To seed the cache at first setup, have `homeos init` (both scaffold and clone modes) write `.last-update-check` with `last_checked_at = now` and `latest_tag = "v" + env!("CARGO_PKG_VERSION")` without making any network call, so the user is not pinged within their first week. The `HOMEOS_SKIP_UPDATE_CHECK` environment variable, when set to any non-empty value, skips both the cache read and the network call entirely (no file write either). When the resolved `latest_tag` differs from the current binary's tag, emit one stderr line: `homeos: v<latest> available — update at https://github.com/hainet50b/homeos`. Use simple tag-equality for the comparison — homeos releases are all `vX.Y.Z` with no pre-release suffix, so semver parsing is not required; should a future tag carry a suffix the worst case is one spurious notification per user, which we accept. Place the call site right before the existing AGENTS.md auto-refresh in `src/commands/cd.rs` so the cd entry sequence stays in one block. Add `.last-update-check` to the `.gitignore` that `homeos init` generates, alongside `state.yml`. Update tests covering: cache-hit-still-fresh (no network), cache-miss-fetch-success (file written with fetched tag), cache-stale-fetch-success (file rewritten with fetched tag), cache-miss-fetch-timeout (file written with current-binary tag and now timestamp), cache-stale-fetch-timeout (file rewritten with previous tag preserved and now timestamp), JSON-corrupt-treated-as-miss, HOMEOS_SKIP_UPDATE_CHECK opt-out (no file write), init-seeded-cache (file exists post-init with expected timestamp and tag), and current-already-latest-no-warning. The `homeos init` test updates should assert `.last-update-check` exists post-init and parses correctly. No README / AGENTS.md changes in this task — the maintainer handles documentation by hand.
- [x] Add a `--print` flag to all cd-family commands (`homeos cd`, `homeos package cd [<package>]`, `homeos plugin cd [<plugin>]`) that prints the absolute path the command would change into and exits 0, without launching a shell and without side effects. In `homeos cd --print`, this means no AGENTS.md auto-refresh and no update-available notice. For `homeos package cd --print` and `homeos plugin cd --print`, the resolved path is the `packages/` or `plugins/` root when no positional argument is given, and the per-entry subdirectory when one is. The flag is for shell-substitution use such as `cd "$(homeos cd --print)"` or `cd "$(homeos package cd neovim --print)"`. The `## homeos cd`, `## homeos package cd`, and `## homeos plugin cd` sections of `SPEC/COMMAND_OUTPUT.md` have been pre-written to capture the text-mode and JSON-mode behaviour and the existing error reasons (`data-dir-not-found`, `package-not-found`, `plugin-not-found`, `directory-not-found`); match those tables exactly. README's Reference subsections for `homeos cd`, `homeos package cd`, and `homeos plugin cd` already mention the flag in the Usage block — keep the `--print` line and ensure the `Options:` section is present. Update tests per command for: text-mode `--print` output (both with and without the optional positional argument, for `package cd` and `plugin cd`), JSON-mode `--print` output, the missing-target error case (only relevant when an argument is given), and confirmation that `--print` writes nothing (no AGENTS.md refresh, no update-check side effects on `homeos cd`).
- [x] Add a `Plugin` column to `homeos package list` text output and a `plugin` field to its JSON output. The text column shows the value of `plugin:` from `homeos.yml` (e.g., `dnf`, `winget`) and renders as `-` when the package has no plugin. The JSON field is the same value as a string, or `null` when absent. Column order in text mode is `Package`, `Enabled`, `Installed`, `Plugin`, `Dependencies`. The motivation is that real-world homeos data directories tend to concentrate on one or two plugins (Windows users mostly on `winget`, Fedora users mostly on `dnf`), so showing the plugin attribution in-line makes it obvious which packages are plugin-backed vs raw scripts and which plugin's behavior change would affect what. The `## homeos package list` section of `SPEC/COMMAND_OUTPUT.md` has been pre-written with the updated text-mode column set and the updated JSON schema (the new `plugin` field is `string | null`); match it exactly. The `homeos package list` section in `README.md` has also been updated with the new column in the sample output and in the descriptive sentence — match those as well. Update tests in `src/commands/package/registry.rs` to cover: text-mode listing including a plugin-backed package, text-mode listing where no package has a plugin (all `-`), JSON-mode output with the new `plugin` field present and `null` when absent, and the existing alphabetical-by-name ordering invariant.
- [x] Add `ls` and `ls-remote` as visible aliases to the list-family subcommands: `homeos package list` accepts `homeos package ls`, `homeos plugin list` accepts `homeos plugin ls`, and `homeos plugin list-remote` accepts `homeos plugin ls-remote`. Use clap's `visible_alias` attribute on the corresponding variants of `PackageCommands` and `PluginCommands` in `src/main.rs` so the aliases surface in `--help` output. The motivation is Unix idiom — `ls` is universal muscle memory for listing, and `ls-remote` matches git's `git ls-remote` precedent. Behavior is unchanged: each alias invokes the same handler as the canonical form and produces byte-identical output. A top-level `homeos ls` shortcut (aliasing `homeos package list`) is intentionally out of scope to keep the `<noun> <verb>` command structure uniform across the CLI. The `## homeos package list`, `## homeos plugin list`, and `## homeos plugin list-remote` sections of `SPEC/COMMAND_OUTPUT.md` have been pre-updated with `Aliases:` notes; match them exactly. The corresponding Reference subsections in `README.md` have also been pre-updated to surface the aliases inside the Usage block; match those as well. Update tests with a smoke assertion per command verifying that the alias form produces output identical to the canonical form (`package ls` ↔ `package list`, `plugin ls` ↔ `plugin list`, `plugin ls-remote` ↔ `plugin list-remote`).
- [x] Fix architecture detection in `install.ps1` to work under Windows PowerShell 5.1. The current detection reads `[System.Runtime.InteropServices.RuntimeInformation]::OSArchitecture`, but on Windows PowerShell 5.1 PSReadLine defines its own `System.Runtime.InteropServices.RuntimeInformation` type with an incomplete member set; type resolution can bind to it, the missing `OSArchitecture` static property silently evaluates to `$null`, `$Arch` stays empty, and the switch falls through to `throw "Unsupported architecture: "`. PowerShell 7 resolves the real type, which is why the bug only surfaces on a fresh machine where the bootstrap runs under the preinstalled 5.1 — exactly the environment `install.ps1` must support (`#Requires -Version 5`). Replace the detection with environment variables, which behave identically on 5.1 and 7+: `$Arch = if ($env:PROCESSOR_ARCHITEW6432) { $env:PROCESSOR_ARCHITEW6432 } else { $env:PROCESSOR_ARCHITECTURE }`, mapping `AMD64` → `x86_64-pc-windows-msvc` and `ARM64` → `aarch64-pc-windows-msvc`, keeping the default-throw with the raw value in the message. The `PROCESSOR_ARCHITEW6432` fallback covers a 32-bit PowerShell process on a 64-bit OS (WOW64), where `PROCESSOR_ARCHITECTURE` reports `x86`. `install.sh` is unaffected.
- [x] Apply the `pwsh` → `powershell` fallback to the cd-family subshell launch. `detect_shell()` in `src/commands.rs` hardcodes `pwsh` as the Windows default when `SHELL` is unset, so `homeos cd`, `homeos package cd`, and `homeos plugin cd` fail with `Error: program not found` on a fresh Windows machine that has only Windows PowerShell 5.1 preinstalled — the same bootstrap failure that the action-execution fallback task already fixed for `.ps1` scripts, just on the subshell path. All three commands resolve their shell through `detect_shell()`, so the fix lands in one place: change `resolve_shell` so that, when `SHELL` is unset on Windows, it resolves to `pwsh` when on `PATH` and `powershell` otherwise. Reuse the existing detection rather than duplicating it — lift `pwsh_on_path()` / `pwsh_on_path_in()` (and the `shell_command_for` selection logic as appropriate) out of `src/commands/package/mod.rs` into a shared location reachable from both `commands.rs` and the package action path, keeping a single source of truth for Windows shell selection. The `SHELL` env var priority is unchanged. The subshell launch emits no fallback notice — Windows PowerShell 5.1 prints its own banner on startup, and the plan-display notice from the action-execution path does not apply because the cd family has no plan. The `Windows shell selection` note under Cross-platform notes in `SPEC/SPEC.md` has been updated to cover both shell-spawning contexts and serves as the specification. No user-visible output changes, so `SPEC/COMMAND_OUTPUT.md` and `README.md` need no updates. Update tests: extend the `resolve_shell` tests in `src/commands.rs` to cover the Windows pwsh-present and pwsh-absent paths, injecting pwsh availability as a parameter (as `shell_command_for`'s tests do) so the tests do not depend on the host machine's `PATH`.
- [x] Statically link the MSVC C runtime into Windows binaries so homeos runs on a fresh Windows machine without the Visual C++ Redistributable. Rust's `*-pc-windows-msvc` targets dynamically link the CRT by default, so the released `homeos.exe` fails to start with a missing-`VCRuntime140.dll` error on a clean Windows install — the DLL ships with the VC++ Redistributable, which is not preinstalled; machines where homeos happens to work simply got the Redistributable from some other software's installer. This is another fresh-machine bootstrap failure in the same family as the PowerShell 7 fallbacks. Fix by adding a `.cargo/config.toml` at the repository root containing a `[target.'cfg(target_env = "msvc")']` section with `rustflags = ["-C", "target-feature=+crt-static"]`, which embeds the CRT into the binary for both `x86_64-pc-windows-msvc` and `aarch64-pc-windows-msvc` release targets (and for local Windows dev builds, keeping dev/release parity). The cost is a few hundred KB of binary size, which we accept. Linux and macOS targets are unaffected because `cfg(target_env = "msvc")` matches neither. No release workflow changes are needed — `cargo build --release --target ...` picks up `.cargo/config.toml` automatically. The `Windows binary linkage` note under Cross-platform notes in `SPEC/SPEC.md` has been added and serves as the specification. No user-visible output changes, so `SPEC/COMMAND_OUTPUT.md` and `README.md` need no updates. Verification: `cargo build` and `cargo test` must pass locally; on Windows, optionally confirm CRT independence with `dumpbin /dependents target\release\homeos.exe` (no `VCRUNTIME140.dll` import; OS-level imports like `KERNEL32.dll` remain and are expected) — if `dumpbin` is not on PATH, the CI Build workflow passing on all targets is sufficient.
- [ ] Enforce Windows CRT independence in CI so the `+crt-static` guarantee cannot silently regress. The `.cargo/config.toml` rustflags can be lost without any visible failure: a future `RUSTFLAGS` env var added to a workflow replaces (does not merge with) config-file rustflags, and cargo's config discovery is cwd-based so an out-of-tree invocation would skip the file entirely. GitHub's Windows runners have Visual Studio installed, so a dynamically-linked binary passes every existing CI job — the regression would surface only as a missing-`VCRuntime140.dll` startup failure on end users' fresh Windows machines, invisible until a bug report arrives. Add an import-table check to the Windows jobs of both workflows: in `.github/workflows/build.yml`, a step guarded by `if: runner.os == 'Windows'` that first runs `cargo build` (the workflow currently only runs fmt/clippy/test, which do not produce `target/debug/homeos.exe`; the debug profile links the same CRT as release, so checking the debug binary is valid) and then inspects it; in `.github/workflows/release.yml`, a step on the two `windows-latest` matrix entries placed after `cargo build --release --target ...` and before `Compress-Archive`, inspecting `target/<target>/release/homeos.exe` so the artifact actually being shipped is what gets verified. The check itself, as an inline `pwsh` step with no third-party actions: locate `dumpbin.exe` via `vswhere.exe` (preinstalled on GitHub Windows runners at `${env:ProgramFiles(x86)}\Microsoft Visual Studio\Installer\vswhere.exe`; use `-latest -products * -find 'VC\Tools\MSVC\**\bin\Hostx64\x64\dumpbin.exe'` and take the first result), run `dumpbin /dependents <exe>`, fail the job if the output matches the case-insensitive pattern `(VCRUNTIME|MSVCP)\d*\.dll|api-ms-win-crt-` (with `+crt-static` the UCRT is statically linked too, so the `api-ms-win-crt-*` forwarder DLLs must also be absent), and echo the dependents list into the job log either way so a failure is immediately diagnosable. OS-level imports (`KERNEL32.dll`, `ADVAPI32.dll`, `ws2_32.dll`, `bcrypt.dll`, `ntdll.dll`, etc.) are expected and allowed. `dumpbin /dependents` parses the PE file regardless of its target architecture, so the same step works unchanged for the cross-built `aarch64-pc-windows-msvc` artifact on the x64 runner. The `Windows binary linkage` note in `SPEC/SPEC.md` has been extended with the enforcement sentence and serves as the specification. No user-visible output changes, so `SPEC/COMMAND_OUTPUT.md` and `README.md` need no updates. Local verification is limited to YAML well-formedness — this host cannot execute the Windows CI jobs, and the Build workflow triggers only on push to `main` / manual dispatch, so the first real run happens during the v0.3.12 release flow, which the maintainer watches.
- [ ] Make test fixture scripts OS-appropriate so the suite passes on a stock Windows machine. The action-script fixtures in `src/commands/package/action.rs` (`fixture_with_script` plus the ~30 inline fixtures) write POSIX sh content (`#!/usr/bin/env sh` + `touch '<marker>'`) into the OS-named script file — which on Windows is `<action>.ps1`, executed under PowerShell. There `touch` resolves only when GNU coreutils happen to be visible from PowerShell: true on GitHub's Windows runners (Git's `usr\bin` is on the system PATH) and on dev machines where Git for Windows was installed with the optional Unix tools, but false on a stock Windows with a default Git install, where 25 tests fail with `The term 'touch' is not recognized`. Introduce a helper next to the fixtures (e.g., `marker_script(marker: &Path) -> String`) that returns PowerShell content on Windows (`New-Item -ItemType File -Path '<marker>' | Out-Null`, single-quoting the path; `Set-Content` is also acceptable) and the existing sh content elsewhere, and use it in every fixture whose script is actually executed. Fixtures that exist to test the unmodified-skeleton detection (`Generated by homeos` marker) must keep their marker comment line — route them through the same helper with the marker line prepended, or keep literal strings where the content itself (not its execution) is the subject of the test. The acceptance criterion is that `cargo test` passes on a clean Windows machine whose `PATH` exposes no GNU coreutils to PowerShell; CI behavior is unchanged (the hidden coreutils dependency simply disappears). No production code changes.
- [ ] Eliminate the process-wide `PATH` mutation in the `shell_command` tests that makes the Windows suite flaky. The two integration tests in `src/commands/package/mod.rs` (`test_shell_command_falls_back_when_pwsh_absent_on_windows` and `test_shell_command_prefers_pwsh_when_present_on_windows`) simulate pwsh absence/presence by rewriting the process-global `PATH` through `EnvVarGuard`. Rust runs every test of the binary in one process on parallel threads, so during the mutation window any concurrently running test that spawns a subprocess resolves binaries against the clobbered `PATH` — observed locally as `plugin refresh` tests panicking at their `Command::new("git")` fixture call with `Err(NotFound, "program not found")`, with the failing set changing from run to run (the signature of a race). `EnvVarGuard` guarantees restoration, not isolation, and edition 2024 marks `std::env::set_var` `unsafe` precisely because of this cross-thread hazard. The selection logic is already fully covered by the parameter-injected tests (`shell_command_for(is_windows, pwsh_available)` and `pwsh_on_path_in(&path)`), which touch no global state; the env-mutating pair only exercises the one-line composition `shell_command()`, so delete both (or rewrite them against the parameterized internals without touching `PATH`). Audit the suite for any other `EnvVarGuard::capture("PATH")` usage and apply the same treatment — at the time of writing these two are the only ones. Do NOT reach for the `serial_test` crate instead: serializing would require annotating every subprocess-spawning test in the suite (50+) and silently breaks again the moment a new spawning test is added. `EnvVarGuard` itself stays for other env vars (e.g., `HOMEOS_DATA_DIR`), which are lower-risk because no concurrent test reads them implicitly during subprocess spawning. Acceptance: repeated full `cargo test` runs on Windows produce no intermittent `program not found` failures in unrelated tests.
- [ ] Execute Windows action scripts with `-NoProfile -File` to make script runs profile-independent. `execute_script` in `src/commands/package/action.rs` currently invokes the shell as `<shell> <script>` with no flags. Unlike non-interactive `sh` (which reads no profile files), `pwsh` and `powershell.exe` load the user's `$PROFILE` even for script invocations, so today every `homeos package install/update/uninstall` / `apply` script run (a) can depend on profile-defined aliases, functions, and env vars — hidden machine-local state that contradicts homeos's reproducibility purpose, (b) pays the profile's startup cost (commonly 0.5–2 s with prompt frameworks and modules) once per script, and (c) mixes profile output and profile errors into script output (observed locally as PSReadLine / `Import-Clixml` cache-corruption noise when many shells start concurrently during tests). Additionally, the bare positional argument is interpreted differently by the two shells — `pwsh` assumes `-File` but `powershell.exe` (the 5.1 fallback) assumes `-Command`, which mis-parses script paths containing spaces — so passing `-File` explicitly also fixes a latent fallback bug. Change the Windows invocation to `<shell> -NoProfile -File <script>`; the Unix `sh <script>` invocation is unchanged. Do NOT add `-NonInteractive` — action scripts may prompt, and stdin/stdout/stderr inheritance is by design. The cd-family interactive subshell (`homeos cd`, `homeos package cd`, `homeos plugin cd`) is the user's own session and must continue to load the profile — out of scope. The `PowerShell profile isolation` bullet under Cross-platform notes in `SPEC/SPEC.md` and the amended script-execution line in its Execution model section serve as the specification. No user-visible output changes, so `SPEC/COMMAND_OUTPUT.md` and `README.md` need no updates. Update tests: extract the invocation construction into a parameter-injected helper (e.g., `script_invocation(shell: &str, script: &Path) -> (program, args)` next to `execute_script`) and assert the Windows form includes `-NoProfile` and `-File` in order, without spawning a process.
